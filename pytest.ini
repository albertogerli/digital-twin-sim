[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    perf: performance SLA tests (time-sensitive; skip under coverage)
    stress: randomized stress tests across scenario axes
